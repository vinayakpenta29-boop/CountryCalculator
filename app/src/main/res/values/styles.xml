<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base app theme -->
    <style name="Theme.CountryCalculator" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryDark</item>
        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="android:statusBarColor">@color/colorPrimaryDark</item>
        <item name="android:navigationBarColor">@color/colorPrimary</item>

        <!-- Default backgrounds use gradient subtly in screens -->
        <item name="android:windowBackground">@drawable/gradient_bg</item>

        <!-- Material components defaults -->
        <item name="materialButtonStyle">@style/Widget.CountryCalculator.Button</item>
    </style>

    <!-- Splash screen theme (full gradient) -->
    <style name="Theme.CountryCalculator.Splash" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/gradient_bg</item>
    </style>

    <!-- Button style with orange/yellow -->
    <style name="Widget.CountryCalculator.Button" parent="Widget.MaterialComponents.Button">
        <item name="android:background">@drawable/btn_primary</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>
